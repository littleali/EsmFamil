alert('s');
input = $('#message');
button = $('#submit');
button.attr('disabled', 'disabled');
button.val('Posting...');
//client = new Faye.Client('/faye');
var publication = window.client.publish('/chat/2', {message: input.val(), created_at: new Date()});
 publication.then(function(){
    input.val('');
    button.removeAttr('disabled');
    button.val("Post");
 }, function(error){
     button.removeAttr('disabled');
     button.val("Try Again");
 });
//publication.errback (function(){
//    button.removeAttr('disabled');
//    button.val("Try Again");
//});
event.preventDefault()
